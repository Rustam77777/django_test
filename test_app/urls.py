from django.urls import path
from test_app import views

urlpatterns = [
    # path('create-product/', views.create_product),
    # path('active-products/', views.active_products_view),
    path('test-run/', views.test_run)

]